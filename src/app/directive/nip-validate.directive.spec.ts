import { NipValidateDirective } from './nip-validate.directive';

describe('NipValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new NipValidateDirective();
    expect(directive).toBeTruthy();
  });
});
